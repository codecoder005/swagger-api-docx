outbound:
  remote-user-api-client:
    url: localhost:18080

spring:
  security:
    user:
      name: user
      password: password
      roles: USER, ADMIN, EMPLOYEE, CLIENT
  cloud:
    compatibility-verifier:
      enabled: false
